create or replace view public.aaaa_pipeline_readiness_v8
with (security_invoker=true)
as
select *
from jsonb_to_record(public.pipeline_readiness_json_v10()) as x(
  formal_article_count integer,
  source_capture_count integer,
  source_page_identity_count integer,
  page_identity_gate text,
  formal_corpus_freeze_gate text,
  formal_corpus_freeze_reason text,
  source_grounded_article_count integer,
  missing_or_invalid_source_region_count integer,
  partition_complete_page_identity_count integer,
  source_region_gate text,
  source_region_gate_reason text,
  total_partition_jobs integer,
  queued_partition_jobs integer,
  running_partition_jobs integer,
  review_partition_jobs integer,
  completed_partition_jobs integer,
  failed_partition_jobs integer,
  pages_needing_secondary_grounding integer,
  articles_needing_secondary_grounding integer,
  strict_embedding_count integer,
  embedding_gate text,
  embedding_gate_reason text,
  duplicate_audit_run_id uuid,
  duplicate_candidate_pair_count integer,
  reviewed_distinct_pair_count integer,
  reviewed_duplicate_pair_count integer,
  unresolved_pair_count integer,
  duplicate_gate text,
  duplicate_gate_reason text,
  profiled_article_count integer,
  categorized_article_count integer,
  no_matching_category_count integer,
  invalid_profile_count integer,
  category_needs_review_job_count integer,
  category_classification_gate text,
  category_gate_reason text,
  total_classification_jobs integer,
  queued_classification_jobs integer,
  running_classification_jobs integer,
  review_classification_jobs integer,
  completed_classification_jobs integer,
  failed_classification_jobs integer,
  valid_scan_count integer,
  valid_theme_proof_count integer,
  total_report_jobs_v6 integer,
  queued_report_jobs_v6 integer,
  running_report_jobs_v6 integer,
  review_report_jobs_v6 integer,
  completed_report_jobs_v6 integer,
  published_report_jobs_v6 integer,
  failed_report_jobs_v6 integer,
  formal_v6_report_count integer,
  readiness_status text,
  active_legacy_cron_count integer,
  orphan_legacy_active_job_count integer,
  legacy_formal_report_count integer,
  invalid_v6_formal_report_count integer,
  externally_executable_security_definer_count integer,
  strict_rls_disabled_table_count integer,
  strict_external_dml_grant_count integer,
  system_safety_gate text,
  readiness_status_v8 text,
  ocr_quality_gate text,
  expected_block_count integer,
  quality_block_count integer,
  missing_quality_count integer,
  stale_quality_count integer,
  provenance_gate text,
  legacy_derivative_sources integer,
  original_preserved_sources integer,
  inventory_gate text,
  inventory_pages integer,
  inventory_jobs integer,
  completed_inventory_jobs integer,
  inventory_discovery_required integer,
  inventory_needs_review integer,
  ocr_readiness_gate text,
  strong_region_count bigint,
  review_region_count bigint,
  low_region_count bigint,
  verified_article_count bigint
);